{
  "name": "yoni-calendar",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dbuild": "docker build -t ical ./",
    "drun" : "docker run --rm -it -p 3000:3000 -v $(pwd)/config.json:/src/app/config.json ical",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cache-manager": "^3.3.0",
    "express": "^4.17.1",
    "express-cache-middleware": "^1.0.1",
    "express-log-url": "^1.4.0",
    "node-fetch": "^2.6.0"
  }
}
